// Application shell. The screens land in step 3 — this placeholder exists so
// the scaffold builds and deploys from day one.

const App = () => (
  <div className="flex min-h-[100dvh] flex-col items-center justify-center px-6 text-white">
    <h1 className="text-center text-5xl font-black italic tracking-tight">
      Flip <span className="text-neon-cyan">Sprint</span>
    </h1>
    <p className="mt-3 text-center text-white/60">
      Pousse ta chance jusqu'à la ligne d'arrivée.
    </p>
  </div>
);

export default App;
